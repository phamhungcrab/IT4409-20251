{
  "ConnectionStrings": {
    // Replace the server and credentials with your own SQL Server instance.
    "DefaultConnection": "Server=localhost;Database=OnlineExaminationDb;Trusted_Connection=True;MultipleActiveResultSets=true"
  },
  // Base logging configuration
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // URL of the frontend client used in CORS policy.  Override in appsettings.Development.json if needed.
  "FrontendUrl": "http://localhost:5173"
}