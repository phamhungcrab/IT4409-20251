# Docker Compose environment (copy to .env)

# Database (SQL Server)
DB_TYPE=sqlserver
DB_HOST=db
DB_PORT=1433
DB_NAME=ExamSystemDB
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd

# Backend
BE_HTTP_PORT=8080
ASPNETCORE_ENVIRONMENT=Development

# Frontend (OnlineExamFe)
FE_PORT=5173
