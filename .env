# Environment configuration for the Online Examination System

# ASP.NET Core settings
ASPNETCORE_ENVIRONMENT=Development

# Database connection string (SQL Server).  Update the server, database
# name, user and password to match your environment.  Set Encrypt
# to false for local development; enable in production.
ConnectionStrings__DefaultConnection=Server=localhost,1433;Database=OnlineExamDb;User Id=sa;Password=a;Encrypt=False

# JWT settings – replace with strong secrets in production
JWT__Secret=super_secret_jwt_key_change_me
JWT__Issuer=OnlineExam
JWT__Audience=OnlineExam
JWT__ExpiresInMinutes=60

# Client settings
VITE_API_BASE_URL=http://localhost:5000/api

# Email (optional) – configure SMTP if sending notifications
SMTP__Host=smtp.example.com
SMTP__Port=587
SMTP__Username=your_username
SMTP__Password=your_password

# Logging
Logging__LogLevel__Default=Information
Logging__LogLevel__Microsoft=Warning